import React, { FunctionComponent } from 'react';
import Header from './Header';

const App:FunctionComponent = () => {
    return (
        <main>
            <Header></Header>
        </main>
    );
}

export default App;